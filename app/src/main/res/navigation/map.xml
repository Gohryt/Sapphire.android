<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/navigation"
    app:startDestination="@id/fragmentUserprofile">

    <fragment
        android:id="@+id/fragmentAuth"
        android:name="com.gohryt.sapphire.FragmentAuth"
        android:label="fragment_auth"
        tools:layout="@layout/fragment_auth">
        <action
            android:id="@+id/action_fragmentAuth_to_fragmentUserprofile"
            app:destination="@id/fragmentUserprofile"
            app:enterAnim="@android:anim/fade_in"
            app:exitAnim="@android:anim/fade_out"
            app:popEnterAnim="@android:anim/fade_in"
            app:popExitAnim="@android:anim/fade_out" />
    </fragment>
    <fragment
        android:id="@+id/fragmentUserprofile"
        android:name="com.gohryt.sapphire.FragmentUserprofile"
        android:label="fragment_userprofile"
        tools:layout="@layout/fragment_userprofile" >
        <action
            android:id="@+id/action_fragmentUserprofile_to_fragmentAuth"
            app:destination="@id/fragmentAuth"
            app:enterAnim="@android:anim/fade_in"
            app:exitAnim="@android:anim/fade_out"
            app:popEnterAnim="@android:anim/fade_in"
            app:popExitAnim="@android:anim/fade_out" />
    </fragment>
</navigation>